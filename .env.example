# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Supabase Configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# File Upload Configuration
MAX_UPLOAD_MB=20

# ===== RAG Configuration (Optional) =====
# These have sensible defaults, only change if needed

# OpenAI Embedding Model
# Options: text-embedding-3-small (1536 dim), text-embedding-3-large (3072 dim)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Document Chunking Configuration
RAG_CHUNK_SIZE=1000          # Characters per chunk
RAG_CHUNK_OVERLAP=200        # Overlap between chunks for context

# Retrieval Configuration
RAG_MAX_RESULTS=5            # Maximum number of chunks to retrieve
RAG_SIMILARITY_THRESHOLD=0.7 # Minimum similarity score (0-1)

# Chat Model Configuration
CHAT_MODEL=gpt-4o            # Model for generating responses (gpt-4o, gpt-4o-mini, etc.)
CHAT_MAX_TOKENS=2000         # Maximum tokens in response
CHAT_TEMPERATURE=0.7         # Response creativity (0-1)
